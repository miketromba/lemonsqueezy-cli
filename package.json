{
	"name": "lmsq",
	"version": "0.1.0",
	"description": "CLI for the Lemon Squeezy API — manage your store, subscriptions, orders, and more from the command line. AI-agent optimized output.",
	"type": "module",
	"bin": {
		"lmsq": "./dist/lmsq.js"
	},
	"files": [
		"dist",
		"README.md",
		"LICENSE"
	],
	"scripts": {
		"test": "bun test",
		"test:watch": "bun test --watch",
		"lint": "biome check .",
		"lint:fix": "biome check --fix .",
		"format": "biome format --write .",
		"typecheck": "bunx tsc --noEmit",
		"dev": "bun run bin/lmsq.ts",
		"build": "bun run scripts/build.ts",
		"build:compile": "bun build --compile bin/lmsq.ts --outfile dist/lmsq",
		"build:compile:all": "bun build --compile bin/lmsq.ts --outfile dist/lmsq-darwin-arm64 && bun build --compile --target=bun-darwin-x64 bin/lmsq.ts --outfile dist/lmsq-darwin-x64 && bun build --compile --target=bun-linux-x64 bin/lmsq.ts --outfile dist/lmsq-linux-x64 && bun build --compile --target=bun-linux-arm64 bin/lmsq.ts --outfile dist/lmsq-linux-arm64 && bun build --compile --target=bun-windows-x64 bin/lmsq.ts --outfile dist/lmsq-windows-x64.exe",
		"prepublishOnly": "bun run build",
		"prepare": "husky"
	},
	"keywords": [
		"lemonsqueezy",
		"lemon-squeezy",
		"cli",
		"billing",
		"subscriptions",
		"payments",
		"saas",
		"license-keys",
		"checkout",
		"api"
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/miketromba/lemonsqueezy-cli"
	},
	"homepage": "https://github.com/miketromba/lemonsqueezy-cli#readme",
	"bugs": {
		"url": "https://github.com/miketromba/lemonsqueezy-cli/issues"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.1",
		"@types/bun": "latest",
		"husky": "^9.1.7"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@lemonsqueezy/lemonsqueezy.js": "^4.0.0",
		"chalk": "^5.6.2",
		"commander": "^14.0.3"
	}
}
